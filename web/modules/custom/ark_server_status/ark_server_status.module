<?php

function ark_server_status_page_top(&$page_top): void {
  $service = \Drupal::service('ark_server_status.helper');
  $serverList = $service->getServerList();
  $serverBoolen = $service->checkServer('servername', $serverList);
}


